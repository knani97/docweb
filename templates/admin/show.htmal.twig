
{% extends 'admin/index.html.twig' %}


{% block content %}
    <h1>User</h1>

    <table class="table">
        <tbody>

        <tr>
            <th>Email</th>
            <td>{{ user.email }}</td>
        </tr>


        <tr>
            <th>Nom</th>
            <td>{{ user.nom }}</td>
        </tr>
        <tr>
            <th>Prenom</th>
            <td>{{ user.prenom }}</td>
        </tr>
        {% if is_granted('ROLE_WORKER') %}
            <th><h4>Curriculum vitae</h4></th>
            <tr>
                <th>Specialité</th>
                <td>{{ cv.specialite }}</td>
            </tr>
            <tr>
                <th>diplome</th>
                <td>{{ cv.diplome }}</td>
            </tr>
            <tr>
                <th>Cv</th>
                <td>{{ cv.file }}</td>
            </tr>
        {% endif %}
        </tbody>
    </table>

    <a href="{{ path('user_index') }}">back to list</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}