{% extends 'DashboardAdmin.html.twig' %}
{% block title %}
    News/Article
{% endblock %}

{% block titredetailsblock %}
{% endblock %}

{% block articleactif %}
    active
{% endblock %}

    {% block titrepage %}
        Liste des articles
    {% endblock %}

    {% block detailtitrepage %}
        L'auteur de la publication sera sanctionné si sa publication enfreint la loi
        (racisme, injure, atteinte à la vie privée...). L'hébergeur sera sanctionné s'il est prouvé qu'il a
        délibérément mis en ligne ou laissé en ligne ce contenu illicite.

    {% endblock %}

{% block details %}

    <form class="col-lg-12">
        {{ form_start(form) }}

        <div class="row">
            <div class="col-md-6">
                <div class="form-group">
                    {{ form_label(form.titre, "Titre", {'label_attr': {'class': 'col-lg-12 bmd-label-floating'}}) }}
                    {{ form_widget(form.titre, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.titre, {'attr': {'class': 'erreur'}}) }}
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-group">
                    {{ form_label(form.id, "Categorie", {'label_attr': {'class': 'col-lg-12 bmd-label-floating'}}) }}
                    {{ form_widget(form.id, {'attr': {'class': 'form-control'}} ) }}
                    {{ form_errors(form.id, {'attr': {'class': 'erreur'}}) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="form-group">
                    {{ form_label(form.text, "Titre", {'label_attr': {'class': 'col-lg-12 bmd-label-floating'}}) }}
                    {{ form_widget(form.text, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.text, {'attr': {'class': 'erreur'}}) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-4">
                <div class="form-group">
                    {{ form_label(form.image, "Image", {'label_attr': {'class': 'col-lg-12 bmd-label-floating'}}) }}
                    {{ form_rest(form, {'attr': {'class': 'bmd-label-floating'}}) }}
                    {{ form_errors(form.image, {'attr': {'class': 'erreur'}}) }}
                </div>
            </div>
        </div>
        <button class="btn btn-primary pull-right">Ajouter categorie</button>
    </form>
{% endblock %}
{% block footer %}
{% endblock %}
