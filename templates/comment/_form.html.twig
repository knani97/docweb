<style>
.row {
    width: 80%;
    margin: auto;
    margin-top: 100px;
}

.btn {
    background-color: #4154f1;
    color: white;
    float: right;
    margin-top: 10px;
}

.form-group {
    margin: 10px;
    margin-bottom: 10px;
}

label {
    font-size: 1rem;
    margin-bottom: 10px;
}
</style>


{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Comment') }}</button>
{{ form_end(form) }}
